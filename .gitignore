*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
*.jks
*.keystore
# google-services.json with real credentials — commit the placeholder only
# Real credentials must be configured via Firebase console + CI secrets
# google-services.json
